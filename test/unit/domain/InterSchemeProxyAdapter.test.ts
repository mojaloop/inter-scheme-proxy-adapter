// import { InterSchemeProxyAdapter, ISPAService, iISPA } from '#src/domain';
// import { createHttpServers } from '#src/infra';
// import { loggerFactory } from '#src/utils';
// import config from '#src/config';

describe('InterSchemeProxyAdapter Tests -->', () => {
  // const logger = loggerFactory('ISPA-test');
  // let proxyAdapter: iISPA;
  //
  // afterEach(async () => {
  //   await proxyAdapter?.stop();
  // });

  test('should handle incoming request', async () => {
    // todo: add actual test
    expect(true).toBeTruthy();
    // const { httpServerA, httpServerB } = createHttpServers(logger);
    // const ispaService = new ISPAService({ logger });
    // const proxyAdapter = new InterSchemeProxyAdapter({
    //   ispaService,
    //   httpServerA,
    //   httpServerB,
    //   logger,
    // });
    //
    // await proxyAdapter.start();
    //
    // const result = await httpServerA.hapiServer.inject({
    //   url: '/path?query=1',
    //   method: 'GET',
    //   headers: {
    //     test: 'x-test',
    //   },
    // });
    // expect(result).toBeTruthy();
  });
});
